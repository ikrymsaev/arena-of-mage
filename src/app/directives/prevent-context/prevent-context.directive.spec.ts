import { PreventContextDirective } from './prevent-context.directive';

describe('PreventContextDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventContextDirective();
    expect(directive).toBeTruthy();
  });
});
